const UnassignedTickets = (): JSX.Element => (
  <div className="w-[90%] h-[40%] bg-white-100 flex flex-col gap-2 px-[2rem] py-[1.3rem]">
    <h1 className="m-0 text-3xl font-bold">Ticket Tracker</h1>
  </div>
);

export default UnassignedTickets;
